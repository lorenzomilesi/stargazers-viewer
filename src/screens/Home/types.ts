import type { StackScreenProps } from '@react-navigation/stack';
import { Routes, StackParamList } from '../../navigation/types';

export type HomeProps = StackScreenProps<StackParamList, Routes.Home>;
